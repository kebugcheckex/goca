import React, { ReactElement } from 'react';
import { Certificate } from '../DataTypes';


type Props = {
  certificate: Certificate;
}

// TODO comme up with a better name, just not to conflict with the Certificate type
function CertificateBox(props: Props) {

}

export default CertificateBox;
